name: core-ts-array CI
on:
  push:
    branches: [main]
    paths:
      - "packages/core-ts-array/**"
      - ".github/workflows/core-ts-array.yml"
      - ".github/workflows/module.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/core-ts-array/**"
      - ".github/workflows/core-ts-array.yml"
      - ".github/workflows/module.yml"

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    secrets: inherit
    with:
      package-path: packages/core-ts-array
