name: core-ts-record CI
on:
  push:
    branches: [main]
    paths:
      - "packages/core-ts-record/**"
      - ".github/workflows/core-ts-record.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/core-ts-record/**"
      - ".github/workflows/core-ts-record.yml"

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/core-ts-record
