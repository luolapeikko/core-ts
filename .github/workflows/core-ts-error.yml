name: core-ts-error CI
on:
  push:
    branches: [main]
    paths:
      - "packages/core-ts-error/**"
      - ".github/workflows/core-ts-error.yml"
      - ".github/workflows/module.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/core-ts-error/**"
      - ".github/workflows/core-ts-error.yml"
      - ".github/workflows/module.yml"

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    secrets: inherit
    with:
      package-path: packages/core-ts-error
